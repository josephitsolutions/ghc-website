# Copy to `.env.local` for local development. Values align with legacy `dbdetails.php`.
DATABASE_HOST=localhost
DATABASE_NAME=mrbonezc_ghc
DATABASE_USER=mrbonezc_cleaner
DATABASE_PASSWORD=

# Optional: Google Maps JavaScript API (Places). Legacy fallback exists in `lib/constants.ts`.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
